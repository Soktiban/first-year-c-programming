#include <stdio.h>
#include <stdlib.h>
#define SIZE 50
/
main() {
	int number[SIZE];/ int.
/
		printf("enter a value for array (-999 to quit)\n\n");
		scanf_s("%i", &number[i]);
		if (number[i] == -999) {
			break; 
	}

	for (i = 0; i < SIZE; i++)
	{
		printf("%i, %i\n", i + 1, number[i]);
		if (number[i] == -999) {
			break;

	}
	system("pause");
}
